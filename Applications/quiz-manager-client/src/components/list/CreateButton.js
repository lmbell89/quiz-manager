import LinkButton from "../common/LinkButton";

const CreateButton = () => {
  return (
    <LinkButton variant="success" to="/create-quiz">
      Create Quiz
    </LinkButton>
  );
};

export default CreateButton;
